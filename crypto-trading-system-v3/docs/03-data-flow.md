# Поток данных

## Два входа в систему

```
┌─────────────┐                                  ┌──────────────────┐
│ Symbol.Tick │─── котировка ──────────────────▶ │ TradingEngine    │
│  (часто)    │                                  │   .OnTick()      │
└─────────────┘                                  └────────┬─────────┘
                                                          │ ДЁШЕВО:
                                                          │ • проверка котировки
                                                          │ • статистика спреда
                                                          │ • обновление MAE/MFE
                                                          │ НИКАКИХ решений
┌─────────────┐                                  ┌────────▼─────────┐
│Bars.BarOpened│── закрывшийся бар ────────────▶ │ TradingEngine    │
│ (редко)     │                                  │  .OnBarClosed()  │
└─────────────┘                                  └──────────────────┘
                                                          │ ВСЯ работа
```

Разделение обязательно (раздел 149): тиков тысячи, баров единицы. Всё, что делается
на каждом тике, должно быть дёшево, иначе робот не успевает в облаке.

## Полный путь бара

```
Candle (закрытый)
   │
   ▼
┌──────────────────────────────────────────────────────────────────┐
│ SymbolDataSet.OnBarClosed(tf, bar)                               │
│                                                                   │
│  ОТКЛОНЯЕТСЯ, если бар:                                          │
│   • некорректен (High < Low, нули, NaN, отрицательный объём)     │
│   • дубликат или из прошлого                                     │
│  Частично применённый бар хуже устаревшего: либо весь ряд        │
│  продвигается, либо ни один индикатор не трогается.              │
│                                                                   │
│  ПОРЯДОК ВНУТРИ:                                                  │
│   1. Donchian ← ПРЕДЫДУЩИЙ бар    (канал описывает прошлое)      │
│   2. истории перцентилей ← значения ДО этого бара                │
│   3. Bars.Add(bar)                                                │
│   4. все индикаторы ← bar                                         │
└───────────────────────────┬──────────────────────────────────────┘
                            ▼
              только для SignalTimeframe
                            │
   ┌────────────────────────┼─────────────────────────┐
   ▼                        ▼                         ▼
AnomalyDetector    FilterValueLedger         CorrelationEngine
.OnBarClosed()     .OnBarClosed(h, l)        .Observe(close)
(счётчик           (продвигает виртуальные    (только на своём
 восстановления)    отклонённые сигналы)       таймфрейме!)
                            │
                            ▼
┌──────────────────────────────────────────────────────────────────┐
│ FeatureEngine.Build(now, data, global, corr, meanCorr)           │
│                                                                   │
│  → null, если истории не хватает.                                │
│    null трактуется как безусловный отказ от торговли,             │
│    а НЕ как «нейтральные признаки».                               │
│                                                                   │
│  Выход: FeatureVector, 60+ полей, все нормированные:             │
│   • расстояния  → в ATR                                          │
│   • уровни      → перцентили 0..1                                │
│   • аномалии    → z-оценки                                       │
│   • время       → sin/cos (час, день недели)                     │
└───────────────────────────┬──────────────────────────────────────┘
                            ▼
┌──────────────────────────────────────────────────────────────────┐
│ StatisticalRegimeModel.Classify(features)                        │
│                                                                   │
│  8 структурных режимов конкурируют за ярлык                       │
│  2 волатильностных — остаточные: «высокая волатильность» —        │
│    это то, что говорят, когда сказать точнее нечего               │
│                                                                   │
│  Гистерезис: новый режим должен выиграть N баров подряд           │
│  Уверенность = сила × отрыв от второго × устойчивость             │
│  Переход → множитель риска ×0.5 до подтверждения                  │
└───────────────────────────┬──────────────────────────────────────┘
                            ▼
┌──────────────────────────────────────────────────────────────────┐
│ RiskEngine.Evaluate(now, account, anomaly, recovery)             │
│  → RiskAssessment: состояние, множитель, что именно блокирует     │
└───────────────────────────┬──────────────────────────────────────┘
                            ▼
┌──────────────────────────────────────────────────────────────────┐
│ ManageOpenPositions()   ← ДО рассмотрения новых входов           │
│  PositionManager.Evaluate() → список действий по приоритету:      │
│   1. инвалидация тезиса  (защитные действия идут первыми)        │
│   2. стоп по времени                                              │
│   3. защита прибыли                                               │
│   4. цели (частичные закрытия)                                    │
│   5. перенос стопа (безубыток → трейлинг)                        │
└───────────────────────────┬──────────────────────────────────────┘
                            ▼
                     ConsiderEntry()
```

## Путь кандидата на вход

```
StrategyContext (только чтение: ни ордеров, ни счёта, ни портфеля)
        │
        ▼
EnsembleVoter.Vote()
        │  • каждая стратегия голосует со знаковой уверенностью
        │  • веса от StrategyWeightEngine
        │  • нормализация + жёсткий потолок 40% на стратегию
        │  • СКИДКА ЗА КОРРЕЛЯЦИЮ: 3 согласных, читающих одно
        │    и то же → 1 эффективный голос
        │  • ШТРАФ ЗА РАЗНОГЛАСИЯ: решение 60/40 слабее единогласного
        ▼
TradeGate.CheckPreTrade()          ← дёшево, отсекает большинство
        │  качество данных → риск → сигнал → режим → уверенность
        │  → аномалии → спред → волатильность → FOMO → погоня
        │  → возраст сигнала → дубликат
        ▼
ExitPlanner.Build()                ← план выхода ДО входа
        │  кандидаты на стоп: ATR / структура / инвалидация / MAE-квантиль
        │  берётся САМЫЙ ЗАЩИЩЁННЫЙ, а не самый узкий
        │  проверка: 0.6..4.0 ATR, издержки ≤ 35% риска
        ▼
CostModel.Estimate()               ← спред + комиссия + проскальзывание
        ▼
BayesianProbabilityModel.Estimate()
        ▼
ExpectedValueEngine.Evaluate()
        │  цель ограничена историческим MFE
        │  EV = p·W − (1−p)·L − C
        │  решение по НИЖНЕЙ ГРАНИЦЕ: EV − z·σ(EV) ≥ требуемое
        ▼
TradeGate.CheckEconomics()
        ▼
PositionSizer.Compute()            ← 9 множителей, все ≤ 1.0
        ▼
TradeGate.CheckPortfolio()         ← лимиты символа, кластера, направления
        ▼
OpportunityRanker.Score()
        ▼
    ПАЧКА КАНДИДАТОВ           ← кандидат НЕ исполняется сразу
        │  бары разных инструментов приходят разными событиями платформы
        │  окно закрывается, как только отчитались все (обычно те же миллисекунды)
        │  страховка по времени: CandidateBatchWindowSeconds
        ▼
OpportunityRanker.Rank()       ← лучшие MaxCandidatesPerCycle, остальным NotRankedHighEnough
        │  перед КАЖДЫМ исполнением лимиты портфеля считаются заново:
        │  первая открытая позиция меняет и риск, и корреляционную картину
        ▼
ExecutionEngine.Open()
        │  1. проверка идемпотентности
        │  2. ОТМЕТКА об исполнении  ← ДО отправки
        │  3. market-range ордер с относительным лимитом проскальзывания
        │  4. повтор только на повторяемых ошибках, ограниченно
        │  5. запись качества исполнения
        ▼
OpenPosition + ExitPlan  →  журнал + сохранение состояния
```

## Обратный поток: закрытие сделки

```
┌──────────────────┐        ┌───────────────────┐
│ Positions.Closed │        │ PositionManager   │
│ (стоп/тейк       │        │ (решение системы) │
│  у брокера)      │        └─────────┬─────────┘
└────────┬─────────┘                  │
         │                            │
         ▼                            ▼
   TradingEngine.OnBrokerPositionClosed / CloseTrade
         │
         │  Прибыль берётся ОТ БРОКЕРА, а не пересчитывается:
         │  его цифра включает реальную цену, комиссию и своп
         │
         │  Пути ДВА, и они могут сработать оба: платформа вправе поднять
         │  Positions.Closed синхронно внутри вызова ClosePosition. Позиция
         │  помечается учтённой при первой записи — иначе одна сделка удвоила
         │  бы свой вес в статистике, в калибровке и в счётчике серии убытков.
         ▼
   ┌─────────────┐
   │ TradeRecord │  ← фиксируется ВСЁ: и результат, и всё,
   └──────┬──────┘     на чём решение принималось
          │
   ┌──────┼──────────────┬─────────────────┐
   ▼      ▼              ▼                 ▼
Performance  Probability  RiskEngine    DecisionJournal
Store        Model        (серии,
(срезы)      (калибровка) кулдаун)
```

### Что переживает рестарт

```
постура риска, серия убытков, кулдаун
дневная и недельная точки отсчёта, исторический пик капитала
открытые позиции с полным контекстом решения
отметки исполнения (защита от дублей)
состояния и веса стратегий
ИСТОРИЯ ЗАКРЫТЫХ СДЕЛОК — настоящих и теневых
```

История сделок — не журнал для чтения, а память системы. Из неё при старте
ВОСПРОИЗВОДЯТСЯ все производные структуры: срезы статистики, бины калибровки,
распределения MAE и MFE, обнаружение деградации стратегий.

Именно воспроизводятся, а не подставляются агрегатами. Сохранять агрегаты значило бы
завести второй способ их получить — и он разошёлся бы с первым при первом же изменении
формулы.

Без этого адаптивный слой существовал бы формально: в облаке процесс перезапускается
регулярно, и каждый раз система начинала бы с априорных значений, никогда ничему не
учась. Теневые сделки сохраняются наравне с настоящими — иначе путь отключённой
стратегии обратно обнулялся бы каждым перезапуском.

Риск-слой через историю НЕ прогоняется: серия убытков и дневная точка отсчёта
восстанавливаются из собственных полей. Прогнать их через историю значило бы заново
«пережить» все прошлые убытки и уйти в остановку на ровном месте.

Снимок ограничен по размеру: хранится `PersistedTradeHistory` последних сделок каждого
вида. Оценки и так взвешены в пользу недавнего, а снимок пишется в `LocalStorage`
платформы — неограниченная история однажды перестала бы туда помещаться, и система
молча потеряла бы ВСЁ состояние, а не только историю.

### Сверка после рестарта

Сопоставить позиции по идентификатору мало. Пока робот был выключен, позиция могла
быть частично закрыта, её стоп мог быть сдвинут или снят вовсе, а сигнал, чей ордер
ушёл в таймаут, остался помеченным как исполненный. Каждое из этих состояний выглядит
рабочим и ведёт к неверным решениям.

Поэтому восстановление доводится до конца ДО первого решения, а не по расписанию сверки:

```
1. курс пересчёта в валюту счёта       ← иначе риск считается в котируемой валюте
2. расхождение объёма                  ← брокер источник правды, расхождение в журнал
3. расхождение стопа                   ← принимается стоп брокера
4. позиция БЕЗ СТОПА у брокера         ← стоп восстанавливается немедленно,
                                          при неудаче — остановка
5. отметки исполнения без позиции      ← ордер не дошёл, сигнал снова доступен
6. полная сверка                       ← сразу, не дожидаясь интервала
```

Пункт 5 разрешает неопределённость, которую нельзя разрешить в момент сбоя. Отметка
ставится ДО отправки ордера и не снимается при таймауте: ответа нет, ордер мог быть
принят, и повтор открыл бы дубль. После рестарта позиции видны — если её нет, значит
ордера не было.

### Позиции, закрывшиеся во время простоя

При восстановлении сохранённые позиции сопоставляются с позициями брокера. Та, которой
у брокера больше нет, — это не расхождение, а закрытая сделка: пока робот был выключен,
сработал стоп, цель или ручное закрытие.

Её исход записывается в статистику КОНСЕРВАТИВНО — как срабатывание стопа. Точная цена
выхода неизвестна, и из двух возможных ошибок выбрана та, что ужесточает постуру риска,
а не ослабляет её на основании догадки. Просто забыть такую позицию нельзя: перезапуск
тогда бесшумно стирал бы проигранные сделки — именно те, которые должны были бы
включить счётчик серии убытков.

## Что НЕ течёт вниз

Намеренно отсутствующие связи, каждая из которых была бы источником ошибок:

* **Стратегии не видят счёт, портфель и открытые позиции.** Их задача — иметь мнение
  о направлении. Позволительность этого мнения решают слои, которые видят книгу целиком.
* **Слои не знают о вышележащих.** `ExitPlanner` не знает, кто его вызвал.
* **Ядро не знает о cAlgo.** Ни одного `using cAlgo` вне папки бота.
* **Виртуальные сделки не попадают в реальную статистику.** Только в теневые срезы.
